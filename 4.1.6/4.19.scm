;; The program should fail loudly and quickly rather than extrapolate
;; upon error; on the other hand, Eva's plan might be implemented by
;; lazy evaluation: promise each internal definition, and evaluate by
;; need.
